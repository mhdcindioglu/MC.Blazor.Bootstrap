<span class="" data-bs-toggle="collapse" data-bs-target="#@id" aria-expanded="false" aria-controls="@id" style="cursor: pointer">
    @CollapseTarget
</span>
<div class="collapse @(Horizontal ? "collapse-horizontal" : "")" id="@id">
    <div class="card card-body" style="width: @Width;">
        @CollapseContent
    </div>
</div>

@code {
    string id = $"collapse-{Guid.NewGuid()}";
    [Parameter] public required RenderFragment CollapseTarget { get; set; }
    [Parameter] public required RenderFragment CollapseContent { get; set; }
    [Parameter] public bool Horizontal { get; set; }
    [Parameter] public string Width { get; set; } = "auto";
}

@* <CollapseTarget ID="id1">
    Open
</CollapseTarget>

<table class="table">
    <tr>
        <td>
            <Collapse>
                <CollapseTarget>
                    Open
                </CollapseTarget>
                <CollapseContent>
                    <div class="card card-body">
                        Some placeholder content for the collapse component. This panel is hidden by default but revealed when the user activates the relevant trigger.
                    </div>
                </CollapseContent>
            </Collapse>
        </td>
    </tr>
</table>

<CollapseContent ID="id1">
    <div class="card card-body">
        Some placeholder content for the collapse component. This panel is hidden by default but revealed when the user activates the relevant trigger.
    </div>
</CollapseContent> *@